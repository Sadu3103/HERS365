# HERS365 Client Environment Variables
# Copy this to .env and update for production

VITE_API_BASE_URL=http://localhost:5000

# Instant Login (Demo) — set to 'true' to show the demo button in non-prod.
# Server also enforces this independently; client flag alone has no effect.
# VITE_ENABLE_DEMO_LOGIN=true
