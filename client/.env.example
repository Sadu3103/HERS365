# HERS365 Client Environment Variables
# Copy this to .env and update for production

VITE_API_BASE_URL=http://localhost:5000

# Instant Login (Demo) — POSITIVE assertion, fails closed.
# Button renders only when BOTH (a) Vite MODE === 'development' (set by
# `vite dev` automatically) AND (b) VITE_ENABLE_DEMO_LOGIN=true.
# Server independently enforces the same gate — client flag alone has
# no effect on auth.
# VITE_ENABLE_DEMO_LOGIN=true
